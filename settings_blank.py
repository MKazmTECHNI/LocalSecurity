# This is prop settings.
# Rename this file into settings.py otherwise it wont work!

allowed_user_id = (
    "YOUR_DISCORD_USER_ID"  # Commands will be accepted only from that user
)
allowed_channel_id = (
    "YOUR_DISCORD_CHANNEL_ID"  # Commands will be accepted only from that channel
)
bot_token = "YOUR_BOT_TOKEN"  # Bot token from discord developer portal
